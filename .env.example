# .env.example
# Your application's environment variables

# Mail configuration for sending password resets and verification emails
# Gmail is supported by default. Use an App Password if using Gmail.
MAIL_USER=
MAIL_PASS=
JWT_SECRET=
